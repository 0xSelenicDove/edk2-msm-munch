name: Build Munch (POCO F4 / Redmi K40S) UEFI

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - 'README.md'
      - '.gitignore'
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Munch UEFI Boot Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Configure Apt Caching & Fast Mirrors
        run: |
          sudo rm -f /etc/apt/apt.conf.d/docker-clean
          echo 'Binary::apt::APT::Keep-Downloaded-Packages "true";' | sudo tee /etc/apt/apt.conf.d/01keep-debs
          echo 'Acquire::ForceIPv4 "true";' | sudo tee /etc/apt/apt.conf.d/99force-ipv4
          echo 'Acquire::http::Timeout "10";' | sudo tee /etc/apt/apt.conf.d/99timeout
          echo 'Acquire::https::Timeout "10";' | sudo tee -a /etc/apt/apt.conf.d/99timeout
          echo 'Acquire::Retries "3";' | sudo tee -a /etc/apt/apt.conf.d/99timeout

      - name: Restore Apt Package Cache
        uses: actions/cache@v4
        with:
          path: |
            /var/cache/apt/archives
            /var/lib/apt/lists
          key: apt-packages-${{ runner.os }}-${{ hashFiles('.github/workflows/build-munch.yml') }}
          restore-keys: |
            apt-packages-${{ runner.os }}-

      - name: Install dependencies
        run: |
          set -e
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            iasl \
            device-tree-compiler \
            abootimg \
            python3-pil \
            python-is-python3 \
            uuid-dev \
            nasm \
            gcc-aarch64-linux-gnu \
            build-essential \
            gettext \
            clang \
            llvm

      - name: Build UEFI Boot Image for Munch
        run: |
          ./build.sh --device munch --release RELEASE

      - name: Build Flashable Installer Zip for Munch
        run: |
          ./build.sh --device munch --installer-zip

      - name: Prepare Artifacts Directory
        run: |
          mkdir -p artifacts
          cp -v Build/munch/RELEASE_CLANG38/FV/SM8250_UEFI.fd artifacts/SM8250_UEFI.fd 2>/dev/null || true
          cp -v boot-*.img artifacts/ 2>/dev/null || true
          cp -v uefi-installer-*.zip artifacts/ 2>/dev/null || true
          ls -lh artifacts/

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: munch-uefi-build-${{ github.sha }}
          path: artifacts/*
          if-no-files-found: error

      - name: Create GitHub Release
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: munch-uefi-${{ github.sha }}
          name: Munch UEFI Bootloader Build (${{ github.sha }})
          body: |
            ## Munch (POCO F4 / Redmi K40S) UEFI Image
            - **Commit**: `${{ github.sha }}`
            - **Device**: Xiaomi POCO F4 / Redmi K40S (`munch`)
            - **Platform**: Qualcomm Snapdragon 870 (`SM8250`)
          files: |
            artifacts/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
